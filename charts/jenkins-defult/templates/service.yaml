apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-defult.fullname" . }}
  labels:
    {{- include "jenkins-defult.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-defult.selectorLabels" . | nindent 4 }}
